const loggen = require('./loggen');

console.log(loggen.gen());

describe('lilog', () => {
	it('should create an http server');
	it('should listen for websocket requests');
	it('should stream additions to file over websocket');
});
